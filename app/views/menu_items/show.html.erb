

  <% if @item.avatar.attached? %>

    <img class="ui left centered large image" src="<%=(url_for(@item.avatar)) %>">
  <% end %>

<table class="ui celled center aligned table">
  <tr>
    <th>Field</th>
    <th>Value</th>
  </tr>


  <tr>
    <td>Item Name</td>
    <td><%= @item.name %></td>
  </tr>

  <tr>
    <td>Description</td>
    <td><%= @item.description %></td>
  </tr>


  <tr>
    <td>Item Price</td>
    <td><%= @item.price %></td>
  </tr>

</table>


<div class="ui grid centered padded"><%= link_to 'Edit', edit_menu_item_path(@item) %> | <%= link_to 'Back', menu_items_path %> | <%= link_to 'Destroy', menu_item_path(@item),
                                                                                                             method: :delete,
                                                                                                             data: { confirm: 'Are you sure?' } %>

</div>